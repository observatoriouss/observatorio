/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['lightsoft.blob.core.windows.net', 'storageuss.blob.core.windows.net', 'raw.githubusercontent.com']
    }
}

module.exports = nextConfig
