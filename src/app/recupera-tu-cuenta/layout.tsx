import ContainerLayout from "@/layouts/container.layout";
import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Recuperación de cuenta | Observatorio tecnológico - USS',
}
export default ContainerLayout