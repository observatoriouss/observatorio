import ContainerLayout from "@/layouts/container.layout";
import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Forma parte de nosotros | Observatorio tecnológico - USS',
}
export default ContainerLayout