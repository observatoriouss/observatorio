import ContainerLayout from "@/layouts/container.layout";
import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Consulta tus Certificados | Observatorio tecnológico - USS',
}
export default ContainerLayout