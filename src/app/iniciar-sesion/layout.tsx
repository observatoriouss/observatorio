import ContainerLayout from "@/layouts/container.layout";
import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Inicia sesión | Observatorio tecnológico - USS',
}
export default ContainerLayout