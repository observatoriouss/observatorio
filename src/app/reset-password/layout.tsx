import ContainerLayout from "@/layouts/container.layout";
import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Resetea tu contraseña | Observatorio tecnológico - USS',
}
export default ContainerLayout